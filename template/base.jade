doctype html

html
    head
        title
            block title
            | · {{ site.title }}

        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(name="author", content="{{ site.configs.author or site.title }}")

        load("/t/elements.scss /t/layout.scss /t/pygments_style.scss")

        block meta
        //- 一个空 block

        if site.configs.google_site_verification
            meta(name="google-site-verification", content="{{ site.configs.google_site_verification }}")
        if site.configs.baidu_site_verification
            meta(name="baidu-site-verification", content="{{ site.configs.baidu_site_verification }}")

    body
        div#main-container
            header#site-header
                block title_in_body
                    h1
                        a(href="/")= site.title
                if site.content
                    p= site.content

            block content

            footer#site-footer
                footer = get_doc('footer.md') or get_doc('footer.txt')
                if not footer
                    p.auto_hide
                        a(href="https://github.com/oyiadin/reos") 模板源码
                        | · 
                        a(href="/template") 克隆主题
                        | · 
                        a(href="/admin") 网站管理
                        | · 
                        a(href="/service/status") 网站状态
                        | · 
                        a(href="/admin/editor") 写文章
                    p
                        | 本站点由
                        a(href="http://farbox.com") FarBox
                        | 强力驱动
                        if site.config.author
                            | ；由 {{ author }} 倾情撰写
                        | 。
                else
                    {{ footer.content }}

                <!--scripts_per_page-->
